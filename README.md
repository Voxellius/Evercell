# Evercell
Customisable and open drop-in firmware and hardware upgrade stack to bring post-2G connectivity to older Nokia phones.

## Building Evercell
To build Evercell, run `./build.sh`. This will build the SDL2 target; running `./targets/sdl2/build/evercell` will start a simulated version of the firmware.
